/*
 * File-based / in-memory storage mode
 * No external database connection is required.
 */

const connectDB = async () => {
    console.log("Using file-based storage. No database connection needed.")
}

export default connectDB